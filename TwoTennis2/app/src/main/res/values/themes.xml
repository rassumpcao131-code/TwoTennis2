<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="EventoArena" parent="Widget.AppCompat.CompoundButton.CheckBox">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:minHeight">42dp</item>
        <item name="android:buttonTint">#FF54C2</item>
        <item name="android:textColor">#F6EAF3</item>
        <item name="android:textSize">14sp</item>
        <item name="android:paddingTop">4dp</item>
        <item name="android:paddingBottom">4dp</item>
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.TwoTennis" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TwoTennis" parent="Base.Theme.TwoTennis" />
</resources>
